services:
  - type: web
    name: lekzy-fx-ai-pro
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: ADMIN_TOKEN
        sync: false
      - key: RENDER
        value: true
